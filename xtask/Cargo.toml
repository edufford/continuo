[package]
name = "xtask"
description = "Workspace tasks that cargo has no command for"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
# A tool for this checkout, not a crate anyone installs.
publish = false

[dependencies]
# For reading `cargo metadata`, which is how the FMU crates are found.
serde_json = { workspace = true }
