name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Lint, test, and smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Tests (includes the determinism smoke test)
        run: cargo test --workspace

      - name: Determinism smoke test (explicit)
        run: cargo test -p continuo-actors --test determinism -- --nocapture

      - name: Run the traffic demo
        run: cargo run -p continuo-examples --example traffic

      - name: Record a run and verify replay of the unmodified log
        shell: bash
        run: |
          cargo run -q -p continuo-examples --example traffic -- --record ci-run.jsonl
          cargo run -q -p continuo-examples --example traffic -- --replay ci-run.jsonl

      - name: Verify replay detects divergence when the log is modified
        shell: bash
        run: |
          # Corrupt one pose x-coordinate mid-run — the first match at or
          # after line 6000, roughly sim time 14 s of 30 — keeping the JSON
          # valid, so replay verifies a long clean prefix before diverging.
          awk 'NR>=6000 && !done && /"x":/ { sub(/"x":[-0-9.eE]*/, "\"x\":99.9"); done=1 } { print }' \
            ci-run.jsonl > ci-tampered.jsonl
          if cmp -s ci-run.jsonl ci-tampered.jsonl; then
            echo "ERROR: sed matched nothing, log was NOT modified - this check would be vacuous"
            exit 1
          fi
          echo "OK: log modified (first pose x-coordinate corrupted to 99.9)"
          if cargo run -q -p continuo-examples --example traffic -- --replay ci-tampered.jsonl; then
            echo "ERROR: replay PASSED on the modified log - divergence was NOT detected"
            exit 1
          fi
          echo "OK: replay failed with nonzero exit - divergence detected as expected"
