[package]
name = "continuo-actors"
description = "Sample actor components for continuo: waypoint paths, controller, physics"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
continuo-core = { workspace = true }
libm = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

# The conductor and the transport serve the determinism integration test,
# and proptest the property tests on the road.
[dev-dependencies]
continuo-conductor = { workspace = true }
continuo-transport = { workspace = true }
proptest = { workspace = true }
serde_json = { workspace = true }
